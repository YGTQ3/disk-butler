# C盘管家 v0.6.2

第四台样本（游戏家用机）带来的一批规则打磨。

## ✨ 清理能力

- **浏览器缓存扩充**：新增支持 CentBrowser、夸克浏览器（Quark），与 Edge/Chrome/360 系一样只清 Cache/Code Cache/GPUCache，不碰账号、密码和历史。

## 🔍 磁盘透视

- **Minecraft 游戏数据保护**：`.minecraft`（存档、材质包、模组）现在明确标为「个人数据·请保留」，防止误清你亲手搭建的世界。

## 🧰 采集器（三版本同步）

- 修复个人数据误标：目录名改为精确匹配，不再把 `ai.opencode.desktop` 这类含 "desktop" 的应用包名误标为个人数据，贡献报告信号更干净。

---

安装包：`DiskButler_0.6.2_x64-setup.exe`（Windows 10/11 x64）
