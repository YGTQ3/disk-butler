# C盘管家 v0.6.1

规则飞轮的第一批果实：一条"指纹规则"看懂所有网页型应用 + 新 Logo 正式启用。

## ✨ 新清理能力

- **网页型应用缓存 (Electron)**：不再逐个软件点名——用 Chromium 缓存指纹（`Cache` 与 `Code Cache`/`GPUCache` 同级并存才认定）自动识别淘宝、千牛等网页内核应用的可重建缓存，只清缓存目录，不碰账号与数据；
- **WPS 旧版本残留**：WPS 升级后旧版本程序目录常年赖着不走（实测一台机器 1.4GB）。以注册表安装版本为锚点精准识别，正在使用的版本绝不列入；
- **360 系浏览器缓存**：360安全浏览器 / 360极速浏览器 / 360极速浏览器X 加入浏览器缓存规则，同样只碰 Cache 三件套，不碰密码、书签和历史。

## 🔍 磁盘透视更聪明

- **孤儿页面文件识别**：非当前使用的 `pagefile.sys`（历史配置留下的旧文件，动辄 10GB+）现在会被标为「历史遗留」并说明来龙去脉；正在使用的页面文件不受影响，仍标「系统文件·请保留」。判定依据系统内核的开机启用清单，拿不准时一律保守。

## 🐛 修复

- 修复清理页「打开回收站」报"找不到文件"的问题（回收站是系统虚拟目录，现已走正确通道打开）。

## 🎨 品牌

- **新 Logo 正式启用**（Z2 Dark：立体 C 盘深色版），应用图标全套更新。

## 🧰 采集器

- 三个版本（软件内置 / 单文件 exe / ps1 脚本）同步新增「多版本并存目录」探测：发现软件升级残留的旧版本目录时自动标记，让每份贡献报告都能带回更多线索。

---

安装包：`DiskButler_0.6.1_x64-setup.exe`（Windows 10/11 x64）
