# C盘管家 v0.7.4 更新说明

> 本次聚焦「一键清理」的系统级能力与体验一致性。所有清理都遵循老规矩：先告诉你「这是什么、删了会怎样」，你点头才动手。

## ✨ 新功能

### 一键清理 · 高级 · 系统临时文件 + 更新缓存
- 新增清理 `C:\Windows\Temp`（系统临时文件，受权限保护、普通清理够不到）和 Windows 更新下载缓存（`SoftwareDistribution\Download`，装完更新残留的安装包）。
- **先只读分析、再决定**：点「分析」会量出各项当前大小、给出「预计可释放」，不到 50MB 会提示"可先不清"，看清收益再操作。
- **安全**：检测到有挂起的 Windows 更新会自动跳过更新缓存、只清临时文件，避免打断更新；清理时短暂停止更新服务、清完自动恢复；正在使用的文件自动跳过、只删内容不删目录。

### 一键清理 · 高级 · 残留检查（体验升级）
- 找出**已卸载软件**留在 AppData/ProgramData 里的遗留文件夹，改为主页面内联展示、可勾选清理；删前可「打开位置」确认，含用户数据的目录会醒目提示先确认。
- 新增网易云/酷狗/飞书/福昕/快手/迅雷等常见软件的残留识别。

### 更多清理规则
- 新增豆包着色器缓存、WPS 多路径缓存等清理项（可安全清、自动重建）。

## 🛡 体验与安全
- 三个高级功能（系统深度清理 / 系统临时文件+更新缓存 / 残留检查）交互统一：先分析/检查 → 看清预计释放 → 确认执行。
- 安装器新增系统版本预检：Windows 7/8 会直接友好提示（本软件需要 Windows 10/11 64 位），不再装完打不开。
- 幕后安全加固：清理/卸载相关的提权流程更严谨。

---
下载：GitHub Releases（`DiskButler_0.7.4_x64-setup.exe`，永久链 `DiskButler-Setup.exe`）。系统要求：Windows 10 / 11（64 位）。
