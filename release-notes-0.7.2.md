# C盘管家 v0.7.2 —— 秒级加速更稳，贡献更省事

围绕上一版的 MFT 秒级扫描做打磨：让"秒级"更稳、更省心。

## ⚡ 秒级加速自检 + 一键修复

- 磁盘透视依赖一个"用完即走"的后台扫描服务来实现免 UAC 秒扫。万一它被安全软件等清掉，扫描会**静默回退到较慢的遍历模式**——以前你不会知道为什么变慢了。
- 现在：磁盘透视页会**自动检测**这个服务在不在，缺失时给出提示，并支持**一键修复**（授权一次即可恢复秒级加速）。结果始终准确，只是快慢之别。

## 🧩 “贡献规则”完整模式升级为秒级

- 软件里「帮它认识更多软件」的**完整模式**，此前收集全盘大目录要几分钟；现在改用内置的秒级扫描引擎，**几十秒即可完成，且无需额外授权**。
- 服务不可用时自动回退到原有方式，功能不中断。

## 🧰 采集器

- 新增可选的独立工具 **DiskButlerCollectorMFT.exe**：秒级枚举全盘大目录，适合愿意用管理员权限跑一次全盘透视的朋友（与免权限的绿色采集器并存，二选一皆可）。

---

安装包：`DiskButler_0.7.2_x64-setup.exe`（Windows 10/11 x64，含扫描服务，一次安装即全部就绪）
