# C盘管家 · 设计宪法 (Design Constitution)

> 一切界面决策以本文件为准。组件样式必须引用 `src/styles/tokens.css` 中的设计令牌，禁止硬编码色值。

## 1. 设计方向：清新亲和风

**给普通人的系统工具**——像"手机管家"一样零压力，而不是像"任务管理器"一样有距离感。

- **浅色为底**：大面积留白 + 柔和底色，信息不拥挤；
- **暖色点缀**：主色是柔和青绿（信任、干净），强调色克制使用；
- **大圆角卡片**：16px 圆角、轻阴影，块与块之间呼吸感留白（24px 起）；
- **大字号人话文案**：正文 14px 起、标题 20px 起；所有文案说人话——"这是什么、能不能动、删了会怎样"，禁止"发现 3000 个垃圾"式恐吓文案；
- **动效克制而有温度**：页面过渡 200ms、TreeMap 布局动画 300ms，缓动 ease-out。

## 2. 色彩系统

### 基础色

| 令牌 | 值 | 用途 |
|---|---|---|
| `--color-bg` | `#F7F9FB` | 页面底色 |
| `--color-surface` | `#FFFFFF` | 卡片表面 |
| `--color-primary` | `#14B8A6` (teal-500) | 主色：按钮、选中态、品牌 |
| `--color-primary-soft` | `#CCFBF1` (teal-100) | 主色浅底：标签、hover |
| `--color-text` | `#1E293B` (slate-800) | 主文字 |
| `--color-text-secondary` | `#64748B` (slate-500) | 次要文字 |
| `--color-border` | `#E2E8F0` (slate-200) | 分隔线、描边 |

### 磁盘分类色彩语义（TreeMap / 水位条 / 徽章统一使用）

| 分类 | 令牌 | 值 | 含义 |
|---|---|---|---|
| 系统 system | `--cat-system` | `#60A5FA` (blue-400) | 操作系统本体，别动 |
| 已装软件 software | `--cat-software` | `#A78BFA` (violet-400) | 软件本体，用卸载移除 |
| 应用缓存 cache | `--cat-cache` | `#FBBF24` (amber-400) | 缓存/临时，多数可清理 |
| 个人文件 personal | `--cat-personal` | `#34D399` (emerald-400) | 你的文件，自己整理 |
| 系统大文件 systemFile | `--cat-system-file` | `#94A3B8` (slate-400) | 休眠/虚拟内存等 |
| 未识别 other | `--cat-other` | `#CBD5E1` (slate-300) | 不确定就别动 |

### 安全等级徽章

| 等级 | 颜色 | 文案 |
|---|---|---|
| safe | 绿 `#10B981` | 可安全清理 |
| caution | 橙 `#F59E0B` | 谨慎操作 |
| keep | 灰 `#64748B` | 请勿删除 |

## 3. 排版与间距

- 字体栈：`system-ui, "Microsoft YaHei UI", "MiSans", sans-serif`（不内嵌字体文件）；
- 字号阶梯：12 / 14 / 16 / 20 / 28 / 36（px），行高 1.5；
- 间距阶梯：4 / 8 / 12 / 16 / 24 / 32 / 48（px）；
- 圆角：卡片 16px、按钮 12px、徽章 999px；
- 阴影：卡片 `0 1px 3px rgb(15 23 42 / 0.06), 0 8px 24px rgb(15 23 42 / 0.04)`。

## 4. 页面信息架构（低保真线框）

### 4.1 磁盘透视页（V0.1 实现）

```
┌────────┬──────────────────────────────────────────────────┐
│        │  磁盘透视                                          │
│  Logo  │  ┌──────────────────────────────────────────────┐ │
│        │  │ [C盘 ▾]  ▓▓▓▓▓▓▓▓░░░ 158/238 GB   [开始扫描]  │ │  ← 盘符+水位条(分类着色)
│ ▣ 透视 │  └──────────────────────────────────────────────┘ │
│ ▢ 清理 │  ┌───────────────────────────────┬──────────────┐ │
│ ▢ 启动 │  │  C:\ > Users > AppData        │  详情卡       │ │  ← 面包屑
│ ▢ 内存 │  │ ┌─────────┬────────┬───────┐  │  ▨ 名称       │ │
│        │  │ │         │        │       │  │  分类徽章     │ │
│ (置灰  │  │ │ TreeMap │        ├───────┤  │  大小/路径    │ │
│ 即将   │  │ │         ├────────┤       │  │  ─────────   │ │
│ 推出)  │  │ │         │        │       │  │  这是什么？   │ │
│        │  │ └─────────┴────────┴───────┘  │  能不能动？   │ │
│        │  └───────────────────────────────┴──────────────┘ │
└────────┴──────────────────────────────────────────────────┘
```

三态：
- **空状态**：居中插画感图标 + "选择磁盘，点击开始扫描" + 大按钮；
- **扫描中**：进度动画 + 已扫描文件数 + 当前目录路径（截断显示）；
- **完成**：TreeMap + 右侧详情卡；hover 出 tooltip，单击下钻，面包屑返回。

### 4.2 一键清理页（V0.2 实现，先定架构）

```
┌──────────────────────────────────────────────┐
│  一键清理                    预计可释放 8.2 GB │
│  ┌────────────────────────────────────────┐  │
│  │ ☑ 临时文件 Temp            2.9 GB      │  │  ← 每项一张卡：
│  │   各程序的临时文件，可安全清理           │  │    名称+大小+人话说明
│  │   删了会怎样：无任何影响                 │  │    +影响说明
│  ├────────────────────────────────────────┤  │
│  │ ☑ npm 缓存                 2.3 GB      │  │
│  │ ☐ 剪映草稿缓存(谨慎)        5.9 GB      │  │  ← 谨慎项默认不勾选
│  └────────────────────────────────────────┘  │
│              [ 清理选中项 ]                   │
└──────────────────────────────────────────────┘
```

原则：白名单制、删除进回收站可撤销、谨慎项默认不勾选、清理后显示前后对比。

## 5. 文案规范

- 用"你"称呼用户，语气友好平实；
- 每个可操作项必须回答三个问题：**这是什么？为什么占空间？动了会怎样？**
- 数字人性化：`1.5 GB` 而非 `1610612736 字节`；
- 禁止：恐吓（"严重威胁"）、夸大（"垃圾堆积如山"）、模糊（"优化系统"）。
