# C盘管家 v0.7.0 —— 磁盘透视，快到秒开

这一版把磁盘透视扫描从"泡杯茶等几分钟"变成了"一眨眼"。

## ⚡ MFT 直读秒级扫描（WizTree 同款原理）

- **全盘扫描进入秒级**：不再逐个目录慢慢数，而是直读 NTFS 主文件表（$MFT）一次性拿到全盘元数据。实测 C 盘 73 万文件 **2.2 秒**扫完（原来要 1~2 分钟）。
- **无需你手动“以管理员身份运行”**：安装时一次性配置好只读扫描服务，之后普通双击打开就能秒扫，不弹 UAC。
- **绝不常驻后台**：扫描服务是"按需启动、用完即走"——只在你点击扫描的那几秒存在，扫完立即退出。任务管理器随时可查，平时零后台进程。这一点我们和"全家桶"软件划清界限。
- **稳妥兜底**：非 NTFS 磁盘、或服务不可用时，自动回退到原有扫描方式，功能永不中断。

## 🔍 磁盘透视 · 更聪明

- **孤儿页面文件识别**：非当前使用的 `pagefile.sys`（历史配置留下的旧文件，动辄 10GB+）会被标为「历史遗留」并说明来龙去脉；正在使用的页面文件不受影响。

## 🧹 清理规则扩充

- 浏览器缓存新增 360 系（安全/极速/极速X）、CentBrowser、夸克，只清缓存不碰账号历史；
- **WPS 旧版本残留**清理（以注册表安装版本为锚点，正在用的版本绝不动）；
- **网页型应用缓存**通用识别（Chromium 指纹，覆盖淘宝/千牛等）；
- 修复清理页「打开回收站」报错。

## 🎨 品牌

- 新 Logo 正式启用（Z2 Dark，立体 C 盘深色版）。

## 🙏 致谢

本版核心的 MFT 秒级扫描引擎与后台服务架构，由社区贡献者 **@challenger000** 通过 PR #1 实现，特此致谢。我们在其基础上按"无常驻进程"原则将服务调整为按需启动、用完即停。

---

安装包：`DiskButler_0.7.0_x64-setup.exe`（Windows 10/11 x64，含扫描服务，一次安装即全部就绪）
